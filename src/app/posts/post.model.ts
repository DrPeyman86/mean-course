//interface is like a class but it can't be instantiated. It's like a contract where it tells what the object
//needs to be, need to have which properties
//this defines what a post needs to be like in the application
export interface Post {
  title: string;
  content: string;
}
